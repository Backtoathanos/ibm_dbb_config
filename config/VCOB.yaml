#
# VCOB processor override for COBOL.yaml (old version of VCOB6)
# No additional steps per requirements; minimal override for future use.
#

version: 1.0.2
tasks:
  - language: Cobol
    variables:
      - name: processorName
        value: VCOB
